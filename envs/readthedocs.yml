name: fpipy-docs
channels:
  - defaults
dependencies:
  - python=3.6
  - sphinx<2.0.0
  - xarray
  - numpy
  - pip
  - pip:
     - colour-demosaicing
     - sphinxcontrib-autoprogram
     - sphinx_rtd_theme
